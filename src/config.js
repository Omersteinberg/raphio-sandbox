export const API_BASE = import.meta.env.VITE_API_BASE || "https://api.raphio.ai/api";

// Flip to true to pause video creation: clicking Create shows a full maintenance
// screen (MaintenanceScreen.jsx) instead of the mode selector. Navbar is untouched.
export const MAINTENANCE_MODE = false;
